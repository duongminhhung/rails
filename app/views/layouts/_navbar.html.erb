<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">MyApp</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to t('navbar.users'), admin_users_path, class: 'nav-link abcd' %>
        </li>
        <li class="nav-item">
          <% if policy(Post).index? %>
            <%= link_to t('navbar.posts'), admin_posts_path, class: 'nav-link' %>
          <% end %>
        </li>

        <% if user_signed_in? %>
          <li class="nav-item">
            <%= form_with url: destroy_user_session_path, method: :delete, class: 'nav-item' do |form| %>
              <%= form.submit t('navbar.logout'), class: 'btn btn-link nav-link' %>
            <% end %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
